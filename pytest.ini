[pytest]
testpaths =
    apps/api/tests
    packages/domain/tests
markers =
    postgres: requires a real PostgreSQL test database; skipped otherwise, and fails instead of skipping when REQUIRE_POSTGRES_TESTS is set
